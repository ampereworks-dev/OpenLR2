I was trying to make skineditor. with winapi (the only GUI I can deal with), it would be a hell so I tried imgui.
but there was a problem. imgui is cannot be compiled on VS08, and DxLib3.02 cannot be compiled on VS17 (it makes LoadGraph() crash on tga file)

so I have decided to make OpenLR2 use recnd DxLib and VS22, and compiled.
here was also another problem. some skin components are in DXArchive(.dxa) file and recnet DxLib cannot read it. old DXA(v1.02) files have to decoded and encoded with v1.10(last version) to be read by DxLib3.24f.
and that will make skins not working on old LR2.
there are DXA encoder/decoder source code so I can handle that but leaves decoded folder.
but today re-think, I can handle it with dxlib3.02 sourcecode.

->solved by building custom dxlib. dxlib3.19f dxarchive_ files into dxlib3.24f